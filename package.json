{
  "name": "eb-app-ui",
  "version": "0.1.0",
  "author": "me@jerahmeelcosinas.net",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sanisoclem/eb-app-ui.git"
  },
  "private": true,
  "scripts": {
    "postinstall": "spago install",
    "clean": "rm -rf node_modules output .spago dist/*.js *.lock .cache",
    "build": "spago build",
    "test": "spago test",
    "esbuild": "esbuild --bundle index.js",
    "bundle": "npm run build && npm run esbuild -- --minify --outfile=dist/index.js",
    "serve": "npm run esbuild -- --servedir=dist --outdir=dist"
  },
  "devDependencies": {
    "esbuild": "^0.14.51",
    "purescript": "^0.15.4",
    "spago": "^0.20.9"
  }
}
